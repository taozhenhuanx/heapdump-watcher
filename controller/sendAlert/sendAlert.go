package sendAlert

import "fmt"

// 根据类型选择告警媒介, 发oss url
func SendAlertType(webhook_type, ossURL string) {
	switch webhook_type {
	case webhook_type:
		fmt.Println(webhook_type)
	case webhook_type:
		fmt.Println(webhook_type)
	case webhook_type:
		fmt.Println(webhook_type)
	default:
		fmt.Println(webhook_type)
	}
}
